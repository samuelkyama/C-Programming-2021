#include <stdio.h>

int main()
{
	int w,l,Area;
	printf("\nEnter value to w:");
	scanf("%d",&w);
	printf("\nEnter value to l:");
	scanf("%d",&l);
	Area=l*w;
	printf("Area=%d",Area);
	return 0;
	}